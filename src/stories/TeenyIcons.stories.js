import React from 'react';

import { ThreeSixty } from '../index';

export default {
  title: 'TeenyIcons/ThreeSixty',
  component: ThreeSixty
};

const Template = (args) => <ThreeSixty />;

export const Default = Template.bind({});