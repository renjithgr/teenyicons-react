import React from 'react';

import { ABTesting } from '../index';

export default {
  title: 'TeenyIcons/ABTesting',
  component: ABTesting
};

const Template = (args) => <ABTesting />;

export const Default = Template.bind({});